export enum SORT_TYPES {
    CREATED = 'CreatedOn',
    MODIFIED = 'ModifiedOn',
    QUEUEING = 'Queueing'
}

export enum ORDER_TYPES {
    DESC = 'DESC',
    ASC = 'ASC'
}

export enum TODOS_VIEW_STATUS {
    ALL = 'All',
    DONE = 'Complete',
    NOTDONE = 'UnComplete'
}
